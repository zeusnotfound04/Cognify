# Production Environment Configuration for Backend
NODE_ENV=production
DATABASE_URL=postgresql://username:password@your-db-host:5432/cognify?schema=public
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRY=7d
GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key-here
MCP_SERVICE_TOKEN=cognify-mcp-service-token-secure-12345
PORT=3001

# Instructions:
# 1. Copy this file to .env
# 2. Update DATABASE_URL with your PostgreSQL connection string
# 3. Generate a secure JWT_SECRET (minimum 32 characters)
# 4. Add your Google Gemini API key for embeddings
# 5. Generate a secure MCP_SERVICE_TOKEN (must match MCP server)
# 6. Ensure your database has the pgvector extension installed