# Backend Service Configuration
BACKEND_URL=http://localhost:3001
MCP_SERVICE_TOKEN=your-secure-mcp-service-token-here

# Optional: Port configuration (defaults to 3002)
# PORT=3002

# Note: The MCP server connects to the backend service, not directly to the database
# The backend service requires its own environment variables:
# - DATABASE_URL: PostgreSQL connection string with vector extension
# - JWT_SECRET: Secret key for JWT token signing
# - GOOGLE_GEMINI_API_KEY: API key for Google Gemini embedding service