# Production Environment Configuration for MCP Server
NODE_ENV=production
BACKEND_URL=https://your-api-domain.com
LOG_LEVEL=warn
MCP_SERVICE_TOKEN=cognify-mcp-service-token-secure-12345

# Instructions:
# 1. Copy this file to .env
# 2. Update BACKEND_URL with your actual server URL
# 3. Generate a secure MCP_SERVICE_TOKEN (minimum 16 characters)
# 4. This token must match the one in your backend .env file